{{!--
Citation for the following html:
Date: 11/02/2025
Copied from: CS340 start app code 
Source URL: https://canvas.oregonstate.edu/courses/2017561/pages/exploration-web-application-technology-2?module_item_id=25645131
--}}
<h1>Reservations</h1>
<div class="pageDescription">
    <p>Reservations page.</p>
</div>

{{! READ table }}
<table>
    <thead>
        {{! For the table header row, we print the DB attribute names }}
        <tr>
            {{#each reservations.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
            <th></th>
        </tr>
    </thead>

    <tbody>
        {{! For each row, print the id, fname, lname, homeworld and age }}
        {{#each reservations}}
        <tr>
            <td>{{this.reservationID}}</td>
            <td>{{this.patronName}}</td>
            <td>{{this.date}}</td>
            <td>{{this.duration}}</td>
            <td>${{this.price}}</td>
            <td>{{this.comments}}</td>
         {{! DELETE form }}
            <form id='delete_person_form'>
                <input type='hidden' name='delete_person_id' value='{{this.reservationID}}' />
                <input type='hidden' name='delete_person_name' value='{{this.price}}' />
                <td>
                    <button type='submit'>
                        Delete
                    </button>
                </td>
            </form>
        </tr>
        {{/each}}
    </tbody>

    
</table>
